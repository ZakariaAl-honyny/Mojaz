version: '3.8'

services:
  DrivingLicenseIssuanceSystem-db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: DrivingLicenseIssuanceSystem-db-prod
    restart: always
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=${DB_PASSWORD}
    volumes:
      - DrivingLicenseIssuanceSystem-db-data:/var/opt/mssql
    networks:
      - DrivingLicenseIssuanceSystem-prod-net
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools/bin/sqlcmd -S localhost -U sa -P ${DB_PASSWORD} -Q 'SELECT 1' || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  DrivingLicenseIssuanceSystem-backend:
    build:
      context: ./src/backend
      dockerfile: Dockerfile
    container_name: DrivingLicenseIssuanceSystem-backend-prod
    restart: always
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ConnectionStrings__DefaultConnection=Server=DrivingLicenseIssuanceSystem-db;Database=DrivingLicenseIssuanceSystemDb;User Id=sa;Password=${DB_PASSWORD};TrustServerCertificate=True
      - Jwt__Key=${JWT_KEY}
      - Firebase__ProjectId=${FIREBASE_PROJECT_ID}
      - SendGridSettings__ApiKey=${SENDGRID_API_KEY}
    depends_on:
      DrivingLicenseIssuanceSystem-db:
        condition: service_healthy
    volumes:
      - DrivingLicenseIssuanceSystem-backend-logs:/app/logs
      - DrivingLicenseIssuanceSystem-backend-uploads:/app/uploads
    networks:
      - DrivingLicenseIssuanceSystem-prod-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  DrivingLicenseIssuanceSystem-frontend:
    build:
      context: ./src/frontend
      dockerfile: Dockerfile
    container_name: DrivingLicenseIssuanceSystem-frontend-prod
    restart: always
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost/api/v1
    depends_on:
      - DrivingLicenseIssuanceSystem-backend
    networks:
      - DrivingLicenseIssuanceSystem-prod-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3

  DrivingLicenseIssuanceSystem-nginx:
    image: nginx:alpine
    container_name: DrivingLicenseIssuanceSystem-nginx-prod
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./cert-placeholders:/etc/nginx/certs:ro
    depends_on:
      - DrivingLicenseIssuanceSystem-frontend
      - DrivingLicenseIssuanceSystem-backend
    networks:
      - DrivingLicenseIssuanceSystem-prod-net

  prometheus:
    image: prom/prometheus:latest
    container_name: DrivingLicenseIssuanceSystem-prometheus
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
    networks:
      - DrivingLicenseIssuanceSystem-prod-net

  grafana:
    image: grafana/grafana:latest
    container_name: DrivingLicenseIssuanceSystem-grafana
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana-data:/var/lib/grafana
    networks:
      - DrivingLicenseIssuanceSystem-prod-net

networks:
  DrivingLicenseIssuanceSystem-prod-net:
    driver: bridge

volumes:
  DrivingLicenseIssuanceSystem-db-data:
  DrivingLicenseIssuanceSystem-backend-logs:
  DrivingLicenseIssuanceSystem-backend-uploads:
  prometheus-data:
  grafana-data:
